<!doctype html>
<!--
  The Wild Fields.

  **The page exists; the thing it describes does not run.** `PlantDetailView`
  already has *Release to the Wild Fields* — a one-second hold that sends a
  plant out of somebody's garden — so the name is the app's own rather than
  invented here. What this page describes beyond that is the proposal
  docs/PHASES.md calls The Winds: a seed set down somewhere and left to wait for
  another.

  **Nothing behind it is built, and that is load-bearing rather than a caveat.**
  `/privacy` says today, truthfully, that no server holds a copy of what you
  grow. A Wild Field is a server holding seeds. The two cannot both be true, and
  the order matters: the privacy page has to be rewritten *before* this opens,
  not after. Until then this page says `notYet`, which is the one sentence that
  keeps the site from contradicting itself. Do not remove it and open the
  feature in the same change.
-->
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
    <title>Peace Garden — the Wild Fields</title>
    <meta name="description" content="Somewhere to send a seed when there is nobody in the room." />
    <meta name="color-scheme" content="dark light" />
    <link rel="stylesheet" href="/assets/site.css" />
    <link rel="icon" href="/assets/icon.svg" type="image/svg+xml" />
    <link rel="apple-touch-icon" href="/assets/icon-180.png" />
  </head>

  <body>
    <div class="pool" aria-hidden="true"></div>

    <main class="page" id="page">
      <header class="masthead">
        <a href="/"
          ><img class="mark" src="/assets/icon.svg" alt="" width="44" height="44"
        /></a>
        <span class="wordmark">Peace Garden</span>
      </header>

      <h1 class="heading" data-s="wildTitle"></h1>

      <section class="about">
        <p class="body" data-s="wildBody"></p>
        <p class="body" data-s="notYet"></p>
      </section>

      <p class="notice body">
        <a href="/privacy" data-s="privacyTitle"></a>
      </p>

      <footer class="foot">
        <label class="chooser">
          <span class="label" id="language-label"></span>
          <span class="chooser__field">
            <select id="language" class="chooser__select"></select>
            <!-- Monoline, even weight, round free ends, per BRAND.md §3.2. -->
            <svg class="chooser__mark" viewBox="0 0 16 16" aria-hidden="true" focusable="false">
              <path
                d="M4 6.5 L8 10.5 L12 6.5"
                fill="none"
                stroke="currentColor"
                stroke-width="1.25"
                stroke-linecap="round"
                stroke-linejoin="round"
              />
            </svg>
          </span>
        </label>
      </footer>
    </main>

    <script type="module" src="/assets/js/plain.js"></script>
  </body>
</html>
